# Copy this to .env, fill it in, and NEVER commit the real .env
# Linux/macOS:  source .env      (or use EnvironmentFile= in systemd)
export GMAIL_ADDRESS="kushalguptadps@gmail.com"
export GMAIL_APP_PASSWORD="xxxxxxxxxxxxxxxx"
export OPENROUTER_API_KEY="xxxxxxxxxxxxxxxx"   # only needed for notegen_hosted.py
